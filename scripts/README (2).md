# 🎬 Multi-Platform Video Uploader

Auto-upload videos to Instagram, YouTube, and TikTok with CSV tracking!

## 📋 System Structure

```
/scripts/
├── run_uploader.bat              ← ЗАПУСК ОТСЮДА (главный скрипт)
├── orchestrator.py               ← Оркестратор (координирует всё)
├── uploader_ig.py                ← Instagram uploader
├── uploader_yt.py                ← YouTube uploader
├── uploader_tt.py                ← TikTok uploader
├── upload_tracker.csv            ← Tracking (создаётся автоматически)
├── youtube_credentials.json      ← YouTube API ключи
├── youtube_token.pickle          ← YouTube токен (создаётся при логине)
├── instagram_session.json        ← Instagram сессия (создаётся при логине)
│
├── videos_queue/                 ← Сюда кладешь видео для загрузки
│   ├── video1.mp4
│   ├── video2.mp4
│   └── ...
│
├── videos_posted/                ← Успешно загруженные видео (автоматически)
└── videos_failed/                ← Видео с ошибками (автоматически)
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```powershell
pip install instagrapi google-auth-oauthlib google-auth-httplib2 google-api-python-client playwright

playwright install chromium
```

### 2. YouTube Setup

1. Перейди на https://console.cloud.google.com/
2. Создай новый проект
3. Включи **YouTube Data API v3**
4. Создай **OAuth 2.0 Client ID** (тип: Desktop application)
5. Скачай JSON и положи как `youtube_credentials.json`

### 3. Переменные окружения (опционально)

```powershell
$env:INSTAGRAM_USERNAME = "your_username"
$env:INSTAGRAM_PASSWORD = "your_password"
$env:TIKTOK_USERNAME = "your_tiktok"
$env:TIKTOK_PASSWORD = "your_tiktok_password"
```

Или напрямую отредактируй в код (uploader_ig.py, uploader_tt.py)

### 4. Запуск

Дважды кликни `run_uploader.bat` или в PowerShell:

```powershell
python orchestrator.py sequential
```

---

## 📊 CSV Структура (upload_tracker.csv)

Создается автоматически, но вот формат:

| Поле | Описание |
|------|----------|
| `Video File` | Имя файла видео (например: `video1.mp4`) |
| `Title` | Заголовок для YouTube |
| `Caption` | Описание для Instagram и TikTok |
| `Description` | Полное описание для YouTube |
| `Tags` | Теги через запятую (например: `travel,viral,nature`) |
| `Status` | `new`, `published`, или `error` |
| `Instagram URL` | Ссылка на пост в Instagram (заполняется автоматически) |
| `YouTube URL` | Ссылка на видео YouTube (заполняется автоматически) |
| `TikTok URL` | Ссылка на видео TikTok (заполняется автоматически) |
| `Error` | Описание ошибки если статус `error` |
| `Timestamp` | Время загрузки |

### Пример CSV:

```csv
Video File,Title,Caption,Description,Tags,Status,Instagram URL,YouTube URL,TikTok URL,Error,Timestamp
video1.mp4,"Amazing Place - 5 Years Left","This place will disappear in 5 years\n\nWhile you're reading this — nobody's there...","Cinematic travel video from Bali","travel,viral,nature,bali",new,,,,,
video2.mp4,"My Gaming Tips","Best Warhammer 40K Tactics 🎮","Tactics for 40K games","gaming,tactics,40k",new,,,,,
```

---

## 🎯 Как использовать

### Шаг 1: Подготовь видео

Положи MP4 видео в папку `videos_queue/`:
```
videos_queue/
├── video1.mp4
└── video2.mp4
```

### Шаг 2: Заполни CSV

Отредактируй `upload_tracker.csv`:
- Заполни поля `Video File`, `Caption`, `Title`, `Tags` и т.д.
- Установи `Status = new` для видео которые нужно загрузить

### Шаг 3: Запусти

Двойной клик на `run_uploader.bat` или:

```powershell
python orchestrator.py sequential
```

Выбери режим:
- **1** = Sequential (безопаснее, по очереди)
- **2** = Parallel (быстрее, одновременно)

### Шаг 4: Проверь результаты

После загрузки CSV обновляется:
- `Status` меняется на `published` или `error`
- Заполняются URL для каждой платформы
- `Error` содержит описание проблемы

---

## 📌 Режимы запуска

### Sequential (Безопасный)
Загружает видео на одну платформу за раз:
1. Instagram ✅ 
2. YouTube ✅
3. TikTok ✅

Медленнее, но надежнее.

```powershell
python orchestrator.py sequential
```

### Parallel (Быстрый)
Загружает одновременно на все платформы (если ресурсы позволяют):

```powershell
python orchestrator.py parallel
```

---

## ⚠️ Важные правила

### Instagram
- Лимит: ~6 видео/день (без ограничений аккаунта)
- Требует сессию для первого логина
- Сохраняет сессию в `instagram_session.json`

### YouTube
- Лимит: ~6 видео/день на бесплатном аккаунте (10,000 units/day)
- Требует `youtube_credentials.json`
- Первый логин откроет браузер (авторизация)
- Токен сохранится в `youtube_token.pickle`

### TikTok
- Использует Playwright (браузерная автоматизация)
- Может потребовать 2FA подтверждение
- При первом запуске браузер видимый (`headless=False`)
- Для скрытого режима меняй в `uploader_tt.py`: `self.headless = True`

---

## 🐛 Troubleshooting

### ❌ "ModuleNotFoundError"
```powershell
pip install instagrapi google-auth-oauthlib google-auth-httplib2 google-api-python-client playwright
```

### ❌ "youtube_credentials.json not found"
1. Скачай из Google Cloud Console
2. Положи в папку со скриптами
3. Переименуй в `youtube_credentials.json`

### ❌ Instagram ошибка "Not logged in"
Удали `instagram_session.json` и попробуй снова (пересвязь).

### ❌ TikTok не логинится
1. Проверь пароль (спецсимволы?)
2. Меняй `self.headless = False` в `uploader_tt.py` для отладки
3. Вручную авторизуйся в браузере

### ❌ "Permission denied" для файла
Закрой файл в другой программе (видео может быть открыто в плеере).

---

## 📱 Статусы видео

| Статус | Что означает | Действие |
|--------|-------------|----------|
| `new` | Видео готово к загрузке | Скрипт загружает |
| `published` | Успешно загружено | Видео перемещено в `videos_posted/` |
| `error` | Ошибка при загрузке | Видео в `videos_failed/`, смотри `Error` колонку |
| `skip` | Пропустить | Скрипт не трогает это видео |

---

## 🔧 Конфигурация

### Изменить режим TikTok с видимым на скрытый

Отредактируй `uploader_tt.py`:
```python
# Было
self.headless = True

# Для отладки (видимый браузер)
self.headless = False
```

### Увеличить задержку между загрузками

Отредактируй `orchestrator.py`:
```python
time.sleep(5)  # Текущая задержка

# Измени на:
time.sleep(10)  # 10 секунд между загрузками
```

### Добавить новые теги по умолчанию

Отредактируй файлы uploaders, найди `tags[:30]` и добавь свои теги.

---

## 📊 Логирование

Все события выводятся в консоль:
- ✅ Успешная загрузка
- ❌ Ошибки с описанием
- 📤 Прогресс загрузки

Результаты сохраняются в CSV!

---

## 🎉 Готово!

Теперь у тебя есть:
- ✅ 3 отдельных uploaders (Instagram, YouTube, TikTok)
- ✅ Главный оркестратор (координирует всё)
- ✅ CSV трекер (отслеживает статусы)
- ✅ BAT лончер (удобный запуск)
- ✅ Sequential и Parallel режимы

Просто положи видео в `videos_queue/`, заполни CSV и запусти! 🚀

---

## 📝 Примечания

- Видео автоматически перемещаются в `videos_posted/` или `videos_failed/` после попытки загрузки
- CSV обновляется после каждого видео
- Сессии сохраняются (не нужно авторизоваться каждый раз)
- Все ошибки логируются в CSV колонке `Error`

Вопросы? Проверь логи в консоли! 💬
